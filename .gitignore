# macOS metadata
.DS_Store

# Ansible retry files
*.retry

# Rendered output of render-test.sh — this is a build artifact, not source.
# The authoritative deck lives in templates/index.html.j2 + templates/slides/*.html.j2
# plus files/app.css and files/app.js.
roles/install_nginx/templates/index.html
