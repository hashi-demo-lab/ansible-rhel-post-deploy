---
collections:
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=6.0.0"
  - name: community.hashi_vault
    version: ">=6.0.0"
  - name: community.vmware
    version: ">=4.0.0"
  # Used by tfc_trigger_apply role to create + apply HCP TF runs.
  # The collection depends on the pytfe Python SDK; tfc_trigger_apply
  # has an ansible.builtin.pip task to install it at runtime if the
  # EE doesn't already ship it.
  - name: hashicorp.terraform
