---
collections:
  - name: ansible.posix
    version: ">=1.5.0"
  - name: community.general
    version: ">=6.0.0"
  - name: community.hashi_vault
    version: ">=6.0.0"
  - name: community.vmware
    version: ">=4.0.0"
  # Resolved from the local AAP Automation Hub (published repo).
  # The collection was uploaded as hashicorp-terraform-2.0.0.tar.gz
  # (built from github.com/hashicorp/terraform-ansible-collection
  # tag 2.0.0) and moved staging -> published.
  - name: hashicorp.terraform
    version: "2.0.0"
